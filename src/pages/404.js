import * as React from "react";
import Layout from "../components/Layout";

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>PAGE NOT FOUND</h1>
      <p>Also, Putin is a bitch.</p>
    </div>
  </Layout>
);

export default NotFoundPage;
